#include<stdio.h>
#include<stdlib.h>

typedef struct node
{
    int data;
    struct node * next;
}node;

int main()
{
    node * head = NULL;
    node * newnode = NULL;
    node * temp; = NULL

    newnode = (node*)malloc(sizeof(node));

    
    return 0;
}